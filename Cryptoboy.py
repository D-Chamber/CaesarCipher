import string


def encrypt(file, key, outfile):
    alphabet = list(string.ascii_uppercase)
    print(file, key, outfile)


def decrypt(file, key, outfile):
    alphabet = list(string.ascii_uppercase)
    print(file, key, outfile)


def crack(file, threshold):
    alphabet = list(string.ascii_uppercase)
    print(file, threshold)


def check():
    pass
